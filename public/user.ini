; ============================================================
; PHP-FPM Configuration (for cPanel / PHP-FPM servers)
; This file overrides php.ini directives at the directory level
; ============================================================

upload_max_filesize = 3M
post_max_size = 5M
max_execution_time = 60
max_input_vars = 5000
memory_limit = 128M
