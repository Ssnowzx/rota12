; ============================================================
; PHP Configuration (cPanel / PHP-FPM)
; Aplica-se a toda a aplicação (raiz, admin, parceiro)
; ============================================================

upload_max_filesize = 3M
post_max_size = 5M
max_execution_time = 60
max_input_vars = 5000
memory_limit = 128M
